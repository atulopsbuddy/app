version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: app-service-taskdef
        LoadBalancerInfo:
          ContainerName: "app"
          ContainerPort: 80
