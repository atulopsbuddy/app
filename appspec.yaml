version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: app-new
        LoadBalancerInfo:
          ContainerName: "nginx"
          ContainerPort: 80
